# Neon Postgres connection string (required for DB)
# Get from https://console.neon.tech → your project → Connection string
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Base URL for paste links (used in POST /api/pastes response)
# Local: set to your dev URL. Vercel: optional, app uses VERCEL_URL when set.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# When set to 1, expiry logic uses x-test-now-ms request header as "current time"
# Leave unset or empty for production.
# TEST_MODE=1
